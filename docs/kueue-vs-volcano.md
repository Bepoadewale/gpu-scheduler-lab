# Kueue vs Volcano

Kueue manages admission, quotas and cohorts for Kubernetes workloads. Kubernetes then assigns Pods to nodes. Volcano adds batch/HPC features such as gang scheduling. They overlap in queues and priority but solve different layers; this lab uses each only where it provides a distinct experiment.
