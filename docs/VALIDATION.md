# Validation

Run `make test lint demo-borrow`. Future execution must include a disposable kind cluster, current Kueue/Volcano version, applied manifests, `kubectl` state evidence, failure experiment, teardown, and environment assumptions. Simulated extended resources never establish hardware behavior; never fabricate validation.
